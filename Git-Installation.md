___

>🪟 Git Installation on ***Windows***:

```
winget install --id Git.Git -e --source winget
```

>🐧 Git Installation on ***Linux / WSL***:

```
sudo apt update && sudo apt upgrade && sudo apt install -y git
```

>🍎 Git Installation on ***macOS***:

```
brew install git
```

___